# Guild-name voting backend. Pinned to compatible ranges; the service is tiny
# and loopback-only, so the surface is small on purpose.
fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
pydantic>=2.5,<3.0
# Gargul SavedVariables (Lua table) parser for the loot-log ingest.
slpp>=1.2,<2.0
# Ed25519 verification of Discord interaction webhooks (officer-vote buttons).
PyNaCl>=1.5,<2.0
